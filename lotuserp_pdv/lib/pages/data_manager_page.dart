import 'package:flutter/material.dart';

class DataManagerPage extends StatelessWidget {
  const DataManagerPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}